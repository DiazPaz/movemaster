# Instalado junto a sparkmax_protocolConfig.cmake (share/sparkmax_protocol/cmake).
# Expone la ruta del JSON de tramas a los paquetes que dependen de éste.
set(sparkmax_protocol_FRAMES_JSON
  "${sparkmax_protocol_DIR}/../spec/spark-frames-2.1.0")
